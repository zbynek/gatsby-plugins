{
  "name": "root",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "dopack": "yarn build; yarn workspaces run -p yarn pack",
    "lint": "yarn workspaces run -p lint",
    "test": "yarn workspaces run -p test",
    "build": "yarn workspaces run -p build"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "0.0.0"
}
