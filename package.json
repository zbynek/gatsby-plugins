{
  "name": "root",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "yarn workspaces foreach -p -v run lint",
    "test": "yarn workspaces foreach -p -v run test",
    "build": "yarn workspaces foreach -p -v run build"
  },
  "workspaces": [
    "packages/*"
  ],
  "version": "1.0.1",
  "devDependencies": {
    "@babel/eslint-parser": "^7.16.3",
    "@babel/eslint-plugin": "^7.14.5",
    "eslint": "^8.3.0",
    "eslint-config-google": "^0.14.0",
    "eslint-plugin-filenames": "latest",
    "eslint-plugin-react": "latest"
  }
}
